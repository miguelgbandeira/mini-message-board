extends layout

block content
  h1= title
  p Welcome to #{title}
  each message in messages
    .message
      p Message: #{message.text}
      p User: #{message.user}
      p Added: #{message.added}

